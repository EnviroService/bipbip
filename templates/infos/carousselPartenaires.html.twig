{#
<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
{% set i=0 %}
{% for organism in organisms %}
    {% set i=i+1 %}

    <ol class="carousel-indicators">
        <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
    </ol>
    <div class="carousel-inner">
        <div class="carousel-item active">

            <img class="d-block w-100" src="/uploads/Logos/Logos/{{ organism.Logo }}" alt="First slide">

        </div>
    </div>
    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>
{% endfor %}
#}




{#<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
    {% set i=0 %}

        <ol class="carousel-indicators">
            {% for organism in organisms %}
            {% set i=i+1 %}
            <li data-target="#carouselExampleIndicators" data-slide-to="{{ i }}" class="active"></li>
            <li data-target="#carouselExampleIndicators" data-slide-to="{{ i }}"></li>
            {% endfor %}
        </ol>

    <div class="carousel-inner">
        {% for organism in organisms %}
        <div class="carousel-item active">
                <a class="linkA" target="_blank" href="{{ organism.organismLink }}"><img title="{{ organism.organismName }}" src="/uploads/Logos/Logos/{{ organism.Logo }}"
                         alt="{{ organism.organismName }}" class="d-block w-100">
                </a>
        </div>
        <div class="carousel-item active">
        <a class="linkA" target="_blank" href="{{ organism.organismLink }}"><img title="{{ organism.organismName }}" src="/uploads/Logos/Logos/{{ organism.Logo }}"
                                                                                 alt="{{ organism.organismName }}" class="d-block w-100">
        </a>
        </div>
                <a class="carousel-control-prev bg-danger" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>

                <a class="carousel-control-next bg-warning" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            {% endfor %}

    </div>
</div>

#}
