{% extends 'admin/index.html.twig' %}

{% block title %}Modififer partenaire{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('admin') }}
    {{ encore_entry_link_tags('editPartner') }}
{% endblock %}

{% block body %}
    <h1 class="editTitle primary-title">Modifier Partenaire</h1>

    {{ include('organisms/_form.html.twig', {'button_label': 'Modifier'}) }}
    <button class="secondary-button">Modifier</button>
    {{ form_end(form) }}

    {{ include('organisms/_delete_form.html.twig') }}

    <a class="secondary-button editLink" href="{{ path('admin_organisms_index') }}">Retour à la liste</a>
{% endblock %}
