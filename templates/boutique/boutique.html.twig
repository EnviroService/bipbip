{% extends 'base.html.twig' %}
{% block title %}
La boutique BipBip
{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('boutique') }}
{% endblock %}
{% block body %}
    <div class="bientot_dispo row">
        <div class="col-12 content-img">
            <img id="" src="{{ asset("/images/Bientot-disponible-bandeau-termine.png") }}" alt="" class="">
        </div>
    </div>

    <div class="row" id="title">
        <h1 class="title-page">
            <span>Achète un BipBip</span>
        </h1>
    </div>

    <main class="container" id="container">

        {% include 'home/5pictos.html.twig' %}

        <div class="row" id="infoBipbip">
            <div class="col-12 mx-auto mt-5 mb-5 text-center">
                <p>
                    Au plus nous collectons de téléphone, plus les prix des green mobiles pourront être avantageux
                    tout en gardant sa qualité absolue !
                </p>
                <p class="blueText">
                    On compte sur toi pour collecter un MAXIMUM de téléphones ! <br>
                    <span>
                        <a href="#">Rejoins notre communauté de Bipers</a>
                    </span>
                </p>
                <p class="secondary-round-title p-2 mt-5">
                    Voici la liste des modèles disponibles
                </p>
            </div>
        </div>

        <div class="container mt-5" id="telephone">
            <h2 class="text-center mb-5">Offre du moment</h2>
            <div class="row" id="telephonePromo">
                {% for promo in promos %}
                    <div class="col-12 col-sm-5 text-center border-left mt-5">
                        <img src="{{ asset(promo.image) }}" alt="téléphone">
                    </div>
                    <div class="col-sm-7 text-center">
                        <div class="text-center mt-5 mb-5">
                            <h4>{{ promo.brand }}</h4>
                            <h5>{{ promo.model }}</h5>
                            <h6>{{ promo.capacity }} GO</h6>
                        </div>
                        <div class="">
                            <p>
                                description
                            </p>
                            <p>
                                {{ promo.description }}
                            </p>
                        </div>
                    </div>
                    <div class="col-12 justify-content-center text-center priceContener">
                        <div class="price border-bottom d-flex">
                            <h3>
                                {{ promo.prix }} €
                            </h3>
                            <a href="{{ path('boutique_show', {id:promo.id}) }}" class="btn btn-success">Voir</a>
                        </div>
                    </div>
                {% endfor %}
            </div>

            <div class="row">
                <h2 class="text-center titleAll mb-0 mt-5 w-100 pt-3">Tous les BipBip disponibles</h2>
            </div>


            <div class="row justify-content-around" id="allPhones">
                {% for phone in phones %}
                        <div class="col-12 col-sm-6 col-md-4 col-lg-3 mt-3 mb-5">
                            <a href="{{ path('boutique_show', {id:phone.id}) }}">
                            <div class="phone text-center">
                                <figure class="mt-3">
                                    <img src="{{ asset(phone.image) }}" alt="">
                                </figure>
                                <div>
                                    <p class="mb-0">{{ phone.brand }}</p>
                                    <p>{{ phone.model }}</p>
                                    <p>Etat: {{ phone.etat }}</p>
                                </div>
                                <div class="priceContent">
                                    <span class="price">{{ phone.prix | number_format(2, '.', ',') }}€</span>
                                </div>
                            </div>
                        </a>
                        </div>
                {% endfor %}
            </div>

            <div class="row mt-5 text-center" id="textAsso">
                <div class="">
                    <p>
                        Quand tu recevras ton green mobile, poste une photo sur Instagram en le mettant bien en avant
                        dans son emballage ouvert <span class="textBlue">#BipBipAttitude</span> et grâce à toi, 1 € sera reversé
                        à <span class="textBlue">une asso</span>
                    </p>
                </div>
            </div>
        </div>

    </main>

{% endblock %}